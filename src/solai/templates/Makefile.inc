# >>> solai autogenerated
bootstrap-solai:
	pipx ensurepath
	@if command -v solai >/dev/null; \
	    then pipx upgrade solai || true; \
	    else pipx install solai; fi
	solai doctor

solai-run:
	solai run
# <<< solai 